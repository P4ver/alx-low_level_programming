#include<stdio.h>
/**
 *main - the entry point
 *
 *print numbers 0 to 9
 *Return: 0 to work successfully
 */
int main(void)
{
	int i;

	for (i = 0; i <= 9; i++)
		printf("%d", i);
	printf("\n");
	return (0);
}
