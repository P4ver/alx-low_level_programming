#include "main.h"
/**
 *mul - function multiplies two integers,
 *@a: the first number to calc
 *@b: the second number to calc
 *Return: the result of multipieling
 */
int mul(int a, int b)
{
	return (a * b);
}
